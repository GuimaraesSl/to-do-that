#!/usr/bin/env sh

# Define a porta padrão
export PORT="${PORT:-3000}"

# Inicia o servidor Rails localmente
echo "Iniciando o servidor Rails em http://localhost:$PORT..."
rails server -b 0.0.0.0 -p $PORT