<div class="cursor-pointer" id="<%= "#{taggable.class.name.downcase}_#{taggable.id}_tag_button" %>" 
      data-tag-editor-target="button">
  <button type="button" class="text-xs text-gray-500 hover:text-info p-1 cursor-pointer"
          data-action="click->tag-editor#showForm"
          title="Adicionar tag">
    <i class="fa-solid fa-tag"></i>
  </button>
</div>